#include<stdio.h>
#include<math.h>
int main()
{
	long long a,b,i,l=0,s=0;
	scanf("%lld%lld",&a,&b);
	for(i>a;i<b;i++)
	{
	 if(i%2==0) s++;  
	 else l++; 
}
     printf("Tegsh too=%lld\n",s);
     printf("Sondgoi too=%lld",l);
}
